[user]
    name = Håken Jevne
    email = hakenkje@gmail.com

[alias]
    co = checkout
    br = branch
    ci = commit
    st = status
    reset-hard = reset --hard HEAD
    lg = log --graph --pretty=format:'%Cred%h%Creset %Cgreen%cr%Creset %Cblue%an%Creset%C(yellow)%d%Creset %s'
    gr = log --graph --simplify-by-decoration --pretty=format:'%Cgreen%ci%Creset  %d' --all
    fixup = "!git log -n 20 --pretty=format:'%h %s' --no-merges origin/master..HEAD | fzf | cut -c -7 | xargs -o git commit --fixup"

[diff]
    color = auto
    tool = vimdiff

[status]
    color = auto
    submoduleSummary = true

[web]
    browser = open

[credential]
    helper = osxkeychain

[push]
    default = simple
    followTags = true

[difftool]
    prompt = false

[merge]
    ff = only

[rebase]
    autosquash = true
